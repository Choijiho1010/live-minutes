# live-minutes 작업 규율

## 프로젝트
온디바이스 실시간 회의 자막·화자분리 + 근거 추적 AI 회의록. macOS(Apple Silicon) 전용.
설계 원본은 로컬 `PLAN.md`(커밋되지 않는다). Phase 에 진입할 때 해당 절만 읽는다 — 통째로 읽지 않는다.

## 확정된 설계 (PLAN.md §4, 마스터 결재 완료 — 이 범위는 묻지 말고 구현한다)
1. 실시간 엔진은 **직접 구현.** 기성 라이브러리에 핵심을 의존하지 않는다
2. **FastAPI + Streamlit 2프로세스 분리**
3. 화자 이름은 **사이드바 상시 패널 + 목소리 등록 DB.** 팝업을 강제하지 않는다
4. 회의록 AI 는 **`claude -p --model haiku`.** 로컬 LLM 은 보류하되 인터페이스는 남긴다
5. 직접 구현이 본체, **diart 는 비교 기준**으로 `.venv-diart` 에 격리
6. 결정·액션·리스크는 클릭하면 **원문 발화 + 음성 재생.** 내보내기 3종, 근거 포함은 선택
7. **합의 검증 — 서로 다른 소속의 화자 2명 이상 + 수락 발화**가 있을 때만 확정
8. 기존 회의록에서 **양식을 역추출.** 여러 개 저장·선택, 근거는 양식과 무관하게 유지

## 판단 경계
- 계획에 없는 **새 설계 판단**(라이브러리 추가·데이터구조·화면구성)은 임의로 정하지 않는다. 선택지와 각 선택의 결과 차이를 붙여 마스터에게 묻는다.
- **순수 구현 디테일**(변수명·함수 분리·테스트 구성)은 스스로 정한다.

## ⚠ 보안 — public 레포다
- `benchmark/gold/` 는 **실제 클라이언트 회의**다. 커밋 금지. `data/`(목소리 임베딩), 오디오 파일, `PLAN.md` 도 마찬가지.
- `.gitignore` + pre-commit 훅(`scripts/block_sensitive.sh`)이 이중 방어. **훅을 우회하지 않는다**(`git add -f`, `--no-verify` 금지).
- 코드·문서·커밋 메시지에 고객사·수행사 실명을 쓰지 않는다. `<고객사>` / `<수행사>` 로 쓴다.
- HF 토큰은 원문을 파일에 적지 않는다. `HF_TOKEN_FILE` 경로만 가리킨다.
- `claude -p` 는 전사 텍스트를 외부로 보낸다. 회사 회의 실사용 시에는 로컬 백엔드로 전환한다.

## 환경
- Python **3.12 고정** (mlx-whisper 0.4.3 + pyannote.audio 4.0.7 검증 조합). uv + `src/` 레이아웃.
- 포트: **API 8000 / UI 8502.** 8501 은 다른 앱이 점유 중. 둘 다 `127.0.0.1` 바인딩.
- 무거운 산출물·venv·모델 캐시는 **`/Volumes/T7` 하위.** 내장 SSD 여유가 빠듯하다.
- 진입점은 T7 마운트를 먼저 확인한다 (`make guard`, `liveminutes.config.require_t7`).

## 게이트
`make lint`, `make typecheck`, `make test` 가 통과해야 커밋한다. CI 도 같은 것을 돌린다.

## 소통
- 호칭은 "마스터". 인사말·군더더기 금지. 최소 문장 최대 밀도.
- 오류 비용이 큰 사실 주장에는 `[확인됨-출처]` / `[추정-미검증]` 라벨.
- 답변은 3섹션 고정: `# Decisions Needed` / `# Action Items` / `# Evidence`. 해당 없는 섹션은 헤더째 생략.
- 물결표 금지(Obsidian 에서 취소선으로 렌더된다). 범위는 하이픈이나 "약"으로.
